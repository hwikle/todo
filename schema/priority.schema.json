{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.invalid/todo/priority.schema.json",
  "title": "Priority",
  "type": "string",
  "enum": ["must", "should", "could"],
  "x-order": ["must", "should", "could"]
}
