#!/bin/zsh
set -eu

ROOT=${0:A:h:h}
PYTHON="$ROOT/.venv/bin/python"

if [[ ! -x "$PYTHON" ]]; then
    print -u2 "Daily TODO dependencies are not installed. Run: $ROOT/bin/setup"
    exit 2
fi

exec "$PYTHON" "$ROOT/lib/todo_config_cli.py" "$@"
